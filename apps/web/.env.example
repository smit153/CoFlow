DATABASE_URL="postgresql://user:pass@host/dbname?sslmode=require"
BETTER_AUTH_SECRET=""       # openssl rand -base64 32
BETTER_AUTH_URL="http://localhost:3000"

# Liveblocks
NEXT_PUBLIC_LIVEBLOCKS_PUBLIC_KEY=""
LIVEBLOCKS_SECRET_KEY=""

# UploadThing (avatar uploads)
UPLOADTHING_TOKEN=""

# Gmail SMTP (for workspace invites)
GMAIL_USER=""
GMAIL_APP_PASSWORD=""

# Gemini API (AI notes generation — not yet wired to any feature, see apps/web/src/lib/gemini.ts)
GEMINI_API_KEY=""

# Paddle (billing — sandbox for now, not yet wired to any feature, see docs/PRD.md §6.12 / P0-15)
PADDLE_API_KEY=""
PADDLE_WEBHOOK_SECRET=""
PADDLE_ENV="sandbox"          # "sandbox" | "production"
PADDLE_PRICE_ID_PRO=""
PADDLE_PRICE_ID_TEAM=""

# Playwright E2E smoke test (optional — defaults to e2e@collabnow.test if unset)
E2E_USER_EMAIL=""
E2E_USER_PASSWORD=""
E2E_BASE_URL=""
